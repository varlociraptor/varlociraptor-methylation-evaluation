Coverage-CpG retention plots for each sequencing platform. As a coverage metric, we use the minimum coverage across both replicates for each CpG position. For Illumina, coverage was computed as the average coverage per replicate across all samples. The y-axis shows the cumulative fraction of CpGs retained in the results at a given coverage level. We excluded coverage values greater than 80 from this plot
